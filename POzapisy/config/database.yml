# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
   username: PO
   password: po
   adapter:  mysql
   database: po
   host:     localhost
   pool: 5
   timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
   username: PO
   password: po
   adapter:  mysql
   database: rubytest
   host:     localhost
   pool: 5
   timeout: 5000

production:
   username: PO
   password: po
   adapter:  mysql
   database: rubyproductiontest
   host:     localhost
   pool: 5
   timeout: 5000
